# 完成报告：单条提问时显示大纲面板

## 一、工单

`docs/工单计划/单条提问大纲显示-工单.md`

## 二、变更摘要

`content.parts/11-outline.js`：`buildOutline` 显示阈值 `items.length < 2` → `items.length < 1`。单个 Q（无小节标题）时面板显示 `Q1 · …`；0 条仍隐藏。

## 三、修改文件清单

| 文件 | 修改 |
|---|---|
| `content.parts/11-outline.js` | 阈值行 + 语义注释（原 :235 附近） |
| `content.parts/00-runtime.js:16` | `VERSION` → `0.3.34` |
| `manifest.json:4` | 版本 → `0.3.34` |
| `popup.parts/00-core.js:1,4` | 头注释与 `POPUP_VER` → `0.3.34` |
| `popup.html:304` | 徽标 → `v0.3.34` |
| `docs/工单计划/单条提问大纲显示-工单.md`、本报告 | 新增 |

## 四、与原计划差异

无。

## 五、自测结果

- `node --check content.parts/11-outline.js` 通过。
- `node scripts/check-line-count.js` 全部文件 ≤ 600 行（本文件 394 行）。
- `node --test tests/*.test.js`：38 通过 / 0 失败。
- `manifest.json` JSON 解析通过。
- 三处版本 + popup 徽标 + 头注释均为 `0.3.34`。

## 六、测试计划执行情况

- 正常路径与边界（空会话、`docdeep_outline=false`）依赖原逻辑分支，静态确认未改动这些分支；实机验证待用户在浏览器重载扩展后确认（1 问短会话应显示 Q1 面板）。

## 七、发现的问题

- 无新增问题。备注：`node --test tests/`（目录参数）在 Node 22 下报模块找不到，需用 `tests/*.test.js` 通配符运行，属调用方式问题，非测试失败。

## 八、遗留事项

- 无。

## 九、影响面评估

仅显示门槛，不影响条目构建/编号/定位/导出；无存储、权限、消息协议变更。

## 十、版本

`0.3.34`

## 十一、回归结果

静态检查与单测全绿；浏览器侧回归（大纲开关、过滤、查找、导出、启停恢复）待用户重载扩展后验收。

## 十二、结论

工单目标达成，等待用户验收。
