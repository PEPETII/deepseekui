# 删除队列完成报告（移除对话队列功能及 popup 队列分区）

## 一、阶段目标回顾

按 `docs/工单计划/删除队列-工单.md`：完整下线 Phase-7「对话队列」——删除 `queue.js`、content 侧运行时与 `#docdeep-queue`、popup「队列」Tab/pane、消息类型 `DOCDEEP_QUEUE` / `DOCDEEP_QUEUE_STATE`、相关单测；popup 分区回到 4 个（阅读 / 模板 / 操作 / 收藏）。版本同步 `0.3.27`。

## 二、已完成任务

- Task 1 删除纯逻辑与 content 队列运行时。
- Task 2 删除 popup 队列分区与交互（UI + 接线 + 样式）。
- Task 3 重建产物与回归：`build-sources` / `check-line-count` / `node --check` / 剩余单测 15/15。
- Task 4 文档：工单 + 本报告 + 规划/AGENTS 同步。

## 三、实际修改内容

- 删除文件：`queue.js`、`content.parts/06-queue.js`、`popup.parts/02-themes-queue.js`、`tests/queue.test.js`、`tests/queue-popup.test.js`。
- 重命名：`content.parts/05-dock-queue.css` → `05-dock.css`（去掉队列样式段）；新增 `popup.parts/02-themes.js`（仅主题 + `paint` 开头）。
- `content.parts/00-runtime.js`：去掉队列状态变量与 `setOn(false)` 清理；`VERSION` → `0.3.27`；头注释去 Phase-7。
- `content.parts/04-rich-editor.js`：发送/停止按钮语义改为纯本地实现；删除 `nativeComposerStopButton`（仅队列消费）。
- `content.parts/08-export.js` / `11-navigation.js` / `12-entry.js` / `01-turns.js`：去掉队列互斥、URL abort、`DOCDEEP_QUEUE` 分支与相关注释。
- `content.parts/09-responsive-print.css`：打印隐藏列表去掉 `#docdeep-queue`。
- `popup.html`：删队列 Tab/pane/`.queue-*` 与 `.ta` 样式；可见版本 → `v0.3.27`。
- `popup.parts/00-core.js` / `03-entry.js`：`POPUP_VER` → `0.3.27`；去掉 `refreshQueue` / 队列按钮接线 / `DOCDEEP_QUEUE_STATE` 监听；导出去掉 `queueRowState/queueMark`。
- `popup.parts/01-bookmarks.js`：把原先被拆到 themes 文件开头的 `handleImportFile` 收尾并回。
- `manifest.json`：版本 `0.3.27`；`content_scripts.js` 去掉 `queue.js`。
- `scripts/build-sources.js`：去掉 `06-queue.js`；改用 `05-dock.css` / `02-themes.js`。
- `AGENTS.md`、`docs/功能扩展规划.md`：去队列化并记录下线。
- 重建：`content.js` / `popup.js` / `content.css`。

## 四、新增文件

- `docs/工单计划/删除队列-工单.md`
- `docs/完成报告/删除队列-完成报告.md`（本文件）
- `popup.parts/02-themes.js`（由原 themes-queue 拆出主题段）

## 五、修改文件

见第三节白名单；确认未改：`background.js`、`rich-model.js`、权限/host、Phase-7 历史工单/报告结论。

## 六、关键实现说明

- 富文本 Enter 发送仍依赖本地 `composerButtonKind`（停止优先于发送），不再委托已删除的 `DocDeepQueue`。
- 队列本就未落盘，无需 storage 迁移；历史 Phase-7 文档保留作考古记录。
- popup 分区 tabs/panes 对齐为 `read/skin/act/mark`。

## 七、测试结果

- `node scripts/build-sources.js`：通过（content 12 parts / popup 4 / css 10）。
- `node scripts/check-line-count.js`：源片段均 ≤600。
- `node --check`：`content.js` / `popup.js` / `background.js` 通过。
- 剩余单测：`rich-model` + `template` + `line-count` 共 15/15 全绿。
- 残留 grep（`*.js/html/css/json`）：`queue|QUEUE|DocDeepQueue|docdeep-queue|queueState|对话队列|Phase-7` 无命中。

## 八、回归测试结果

- 走读级通过：四分区、主题模板、导出/查找/大纲/富文本发送路径未再引用队列状态。
- 需 Chrome/Edge 人工补测：重载扩展后 popup 无「队列」tab；页面无右下角队列面板；富文本发送与导出正常。

## 九、发现的问题

- 编辑部分源文件时偶发 `EBUSY`（文件占用），改用一次性 purge 脚本完成删除；临时脚本已清理。
- 无阻断级功能问题。

## 十、与原计划差异

- 无。按工单白名单执行；历史 Phase-7 工单/报告保留不改写。

## 十一、未完成项

- 无代码未完成项。实机页面回归待用户验收。

## 十二、风险与后续建议

- 重载扩展前，旧 content script 可能仍短暂持有队列代码；验收前请在扩展管理页点「重新加载」。
- 若将来恢复队列，应以 Phase-7 文档为考古基线另开新工单，勿直接复活已删代码假设。

## 十三、版本与同步

- `manifest.json` / `POPUP_VER` / `VERSION` / popup 可见版本均为 `0.3.27`。

## 十四、验收结论

代码与文档侧 DoD 已达成；等待用户实机验收确认。
