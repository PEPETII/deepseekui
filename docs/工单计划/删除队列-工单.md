# 删除工单：移除「对话队列」功能及 popup 队列分区

## 一、删除目标

完整下线 Phase-7「对话队列」：删除 `queue.js` 纯逻辑层、content 侧队列运行时与 `#docdeep-queue` 注入面板、popup「队列」Tab/pane 与样式、消息类型 `DOCDEEP_QUEUE` / `DOCDEEP_QUEUE_STATE`、相关单测；popup 分区回到 4 个（阅读 / 模板 / 操作 / 收藏）。版本同步 `0.3.27`。

## 二、本次删除涉及的功能

- 对话队列全链路：入队解析、状态机、采样推进、暂停/继续/清空、页面进度面板、popup 录题分区。
- 消息协议：`DOCDEEP_QUEUE`、`DOCDEEP_QUEUE_STATE`。
- 测试：`tests/queue.test.js`、`tests/queue-popup.test.js`。
- 构建清单：`scripts/build-sources.js` 去掉 `content.parts/06-queue.js`；`manifest.json` 去掉 `queue.js`。
- 文档：`docs/功能扩展规划.md` 功能 15 标「已下线」；`AGENTS.md` 去队列化；本工单 + 完成报告。

## 三、本次不删除的内容

- Phase-6 富文本发送原语：`nativeComposerSendButton` / `composerButtonKind` / `composerButtonDisabled` 等仍服务 WYSIWYG Enter 发送，仅去掉对 `DocDeepQueue` 的委托，保留本地降级实现。
- `#docdeep-dock` / `#docdeep-tools` 工具条样式与逻辑。
- Phase-7 历史工单与完成报告（只追加下线记录，不改写历史结论）。
- `background.js`、`rich-model.js`、权限/host、`docs/known-issues.md` 历史条目结论。
- 阅读 / 模板 / 操作 / 收藏、导出、查找、大纲、键盘导航等其余功能。

## 四、前置条件

- 用户明确要求：删除项目内队列功能，同步清理代码逻辑，并删除扩展的队列 tab 界面。
- 用户要求新建工单记录当前计划后再动手。

## 五、代码影响范围（白名单）

- 允许删除/修改：
  - `queue.js`（整文件删除）
  - `content.parts/06-queue.js`（整文件删除）
  - `content.parts/00-runtime.js`（队列状态变量与 `setOn(false)` 清理）
  - `content.parts/04-rich-editor.js`（去掉 `queueApi` 委托，保留本地按钮语义）
  - `content.parts/05-dock-queue.css`（删 `#docdeep-queue` 样式段；可改名为 `05-dock.css`）
  - `content.parts/08-export.js` / `11-navigation.js` / `12-entry.js` / `01-turns.js`（去队列互斥与消息分支）
  - `content.parts/09-responsive-print.css`（打印隐藏列表去 `#docdeep-queue`）
  - `popup.html`（队列 Tab/pane/样式）
  - `popup.parts/00-core.js` / `02-themes-queue.js` / `03-entry.js`（去队列逻辑；主题文件可改名为 `02-themes.js`）
  - `popup.parts/01-bookmarks.js`（顺手把被拆到 themes 文件开头的 `handleImportFile` 收尾并回）
  - `manifest.json`、`scripts/build-sources.js`
  - `tests/queue.test.js`、`tests/queue-popup.test.js`（删除）
  - `AGENTS.md`、`docs/功能扩展规划.md`、本工单、完成报告
- 禁止：`background.js`、`rich-model.js`、`manifest.json` 权限/host、改写 Phase-7 历史结论。

## 六、任务拆解

### Task 1 删除纯逻辑与 content 队列运行时

内容：删 `queue.js`、`content.parts/06-queue.js`；runtime 去掉队列变量与关闭清理；export/navigation 去掉运行中互斥与 URL abort；entry 去掉 `DOCDEEP_QUEUE`；rich-editor 按钮语义只走本地实现；dock CSS 去掉队列规则；打印规则去 `#docdeep-queue`；`build-sources.js` / `manifest.json` 同步。

验收：源码与产物中无 `DocDeepQueue` / `DOCDEEP_QUEUE` / `#docdeep-queue` / `queueState`；`node --check` 通过。

### Task 2 删除 popup 队列分区

内容：`popup.html` 删队列 Tab、pane、`.ta`/`.queue-*` 样式；`popup.parts` 删队列渲染/动作/监听/导出；`load()` 不再 `refreshQueue()`；分区回到 4；可见版本与 `POPUP_VER` → `0.3.27`。

验收：tabs/panes 对齐为 `read/skin/act/mark`；无 `queue-start` 等悬空 id；`node --check popup.js` 通过。

### Task 3 重建产物与回归

内容：`node scripts/build-sources.js`；`node scripts/check-line-count.js`；跑剩余单测；全库残留 grep。

验收：构建成功；无队列测试残留；其余测试绿；三处版本 `0.3.27` 一致。

### Task 4 文档

内容：本工单；完成报告；`docs/功能扩展规划.md` 功能 15 标已下线并追加路线图；`AGENTS.md` 去队列描述。

验收：规划可查下线状态与工单/报告链接；AGENTS 与代码一致。

## 七、测试计划

- 删除验证：popup 无「队列」tab；页面无 `#docdeep-queue`；`manifest` 不再加载 `queue.js`；发送 `DOCDEEP_QUEUE` 无处理方。
- 边界：富文本 Enter 发送仍可用（本地按钮语义）；导出/补全不再出现队列互斥文案；老用户无队列存储键可清（本功能本就未落盘）。

## 八、回归测试范围

开关启停、字号/主题/大纲、模板卡与主题下拉双向一致、复制全文、导出 MD/JSON/HTML、打印、查找、收藏增删改查/导入导出、富文本输入与格式工具栏、键盘导航。

## 九、完成定义 Definition of Done

- 白名单 diff 可 Review；无队列运行时代码残留；版本三处 `0.3.27`。
- 构建与剩余单测通过；文档与 AGENTS 同步；完成报告基于真实 diff。
- popup 四分区可用，无队列入口。

## 十、交付与验收条件

- 先完成代码、验证和报告，再向用户汇报。
- 汇报后停止，等待用户验收；未经确认不恢复已删功能、不开新阶段。
